# OpenProof C++ SDK

C++26 module SDK exported as `openproof.sdk`. It creates PKCE/state/nonce
material and typed authorization-code, refresh-token and UserInfo requests while
keeping bearer material in `foundation::SecretString`.
